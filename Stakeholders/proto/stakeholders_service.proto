syntax = "proto3";

option go_package = "proto/auth";

service StakeholderService {
  rpc Greet(Request) returns (Response) {}
}

message Request {
  string name = 1;
}

message Response {
  string greeting = 2;
}